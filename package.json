{
  "name": "block-script-compiler",
  "version": "0.0.1",
  "eslintConfig": {
    "parserOptions": {
      "ecmaVersion": 6,
      "sourceType": "module"
    },
    "presets": [
      "es2015"
    ]
  },
  "scripts": {
    "lint": "eslint src/ --ext .js",
    "compile": "nodemon run.js"
  },
  "dependencies": {
    "async": "^2.0.1"
  },
  "devDependencies": {
    "eslint": "^2.0.0",
    "nodemon": "^1.10.2"
  }
}
